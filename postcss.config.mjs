/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    
  '@tailwindcss/postcss': {}, // Add this and configure as needed
  // autoprefixer: {} // Keep other plugins
  },
};

export default config;
